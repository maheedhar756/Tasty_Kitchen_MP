import { Circles } from 'react-loader-spinner'

const Loader = () => (
  <div className="flex justify-center items-center h-screen">
    <Circles color="#EF4444" height={80} width={80} />
  </div>
)

export default Loader
